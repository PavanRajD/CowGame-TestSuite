// Defining a contract for a class that will implement it.
public interface SearchAlgorithm {
    /**
     * This function takes a farm and returns a farm.
     * 
     * @param farm The farm object that contains the search criteria.
     * @return A Farm object.
     */
    public Farm performSearch(Farm farm);
}
